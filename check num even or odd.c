#include<stdio.h>
#include<conio.h>
int main ()
{
	int x;
	printf("\n Enter two integer");
	scanf("%d",&x);
	if(x%2==0)
	printf("%d is the Enven number",x);
		else
			printf("%d is the ODD number",x);
			return 0;
}
