#include<stdio.h>
#include<conio.h>
int main ()
{
	int x,y;
	printf("\n Enter two integer");
	scanf("%d%d",&x,&y);
	if(x%y==0)
	printf("%d is divisible by %d",x,y);
		else
		printf("%d is divisible by %d",x,y);
			return 0;
}
